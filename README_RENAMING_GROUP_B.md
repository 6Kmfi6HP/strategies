# Quick Reference - Group B Renaming

## Quick Start

Execute all renames with one command:

```bash
cd C:/Users/liang/GitHub/strategies
bash rename_all_group_b.sh
```

Or execute individually:

```bash
bash rename_bollinger.sh   # 198 files
bash rename_atr.sh          # 96 files
bash rename_cci.sh          # 55 files
bash rename_kdj.sh          # 50 files
```

## File Statistics

| Directory | Files | Script |
|-----------|-------|--------|
| 05-技术指标-布林带 | 198 | rename_bollinger.sh |
| 06-技术指标-ATR | 96 | rename_atr.sh |
| 07-技术指标-CCI | 55 | rename_cci.sh |
| 08-技术指标-KDJ | 50 | rename_kdj.sh |
| **TOTAL** | **399** | rename_all_group_b.sh |

## Naming Convention

```
序号-中文名-英文名.md
```

Examples:
- `001-布林带突破策略-bollinger-bands-breakout-strategy.md`
- `025-动态趋势跟踪-atr-dynamic-trend-following.md`
- `010-零点反转策略-cci-zero-cross-strategy.md`

## Files Generated

1. **rename_bollinger.sh** - Bollinger Bands renaming (198 commands)
2. **rename_atr.sh** - ATR renaming (96 commands)
3. **rename_cci.sh** - CCI renaming (55 commands)
4. **rename_kdj.sh** - KDJ renaming (50 commands)
5. **rename_all_group_b.sh** - Master script (executes all 4)
6. **process_renames_final.py** - Python generator script
7. **RENAMING_SUMMARY_GROUP_B.md** - Detailed summary
8. **README_RENAMING_GROUP_B.md** - This quick reference

## Verification

After running scripts:

```bash
# Check what was renamed
git status

# See name changes
git diff --name-status

# Count renamed files
git status --short | wc -l
```

Expected: 399 files with status 'R' (renamed)

## Safety Features

- Uses `git mv` to preserve file history
- All scripts include error checking
- Master script asks for confirmation
- Can be run individually or together

## Troubleshooting

If you need to undo:

```bash
git reset --hard HEAD
```

If you want to see what would happen without executing:

```bash
# Remove 'git mv' and replace with 'echo' in scripts
sed 's/^git mv/echo git mv/' rename_bollinger.sh | bash
```

---

**Generated by Agent 3 on 2025-11-05**
